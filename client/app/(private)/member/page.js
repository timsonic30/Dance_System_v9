"use client";
import { useRouter } from "next/navigation";

export default function Member() {
  const router = useRouter();

  return (
    <>
      <div>This is the member page, need to router</div>
    </>
  );
}
